---
import BaseLayout from '../layouts/BaseLayout.astro';
import { siteConfig } from '../config/site';
---

<BaseLayout 
  title="学習資産" 
  description="PT国試コミュニティの学習資産ハブ。テンプレート、要点まとめ、学習ガイドなど、国試対策に必要な資料にアクセスできます。"
>
  <section class="resources-hero">
    <div class="container text-center">
      <h1 class="resources-hero__title">学習資産ハブ</h1>
      <p class="resources-hero__subtitle">
        国試対策に必要な資料がすべてここに
      </p>
    </div>
  </section>
  
  <section class="section">
    <div class="container">
      <div class="resources-grid">
        <div class="resource-card">
          <span class="resource-card__icon">📋</span>
          <h3 class="resource-card__title">学習計画テンプレート</h3>
          <p class="resource-card__desc">
            年間・月間・週間の計画表、進捗管理シートなど
          </p>
        </div>
        
        <div class="resource-card">
          <span class="resource-card__icon">📚</span>
          <h3 class="resource-card__title">要点まとめ資料</h3>
          <p class="resource-card__desc">
            科目別・テーマ別の重要ポイントをまとめた資料
          </p>
        </div>
        
        <div class="resource-card">
          <span class="resource-card__icon">🗂️</span>
          <h3 class="resource-card__title">暗記カード</h3>
          <p class="resource-card__desc">
            繰り返し学習に使える暗記カードデータ
          </p>
        </div>
        
        <div class="resource-card">
          <span class="resource-card__icon">📝</span>
          <h3 class="resource-card__title">過去問分析シート</h3>
          <p class="resource-card__desc">
            過去問の傾向分析や間違いノートのテンプレート
          </p>
        </div>
        
        <div class="resource-card">
          <span class="resource-card__icon">🏥</span>
          <h3 class="resource-card__title">実習準備資料</h3>
          <p class="resource-card__desc">
            臨床実習に向けた準備チェックリストやガイド
          </p>
        </div>
        
        <div class="resource-card">
          <span class="resource-card__icon">💼</span>
          <h3 class="resource-card__title">キャリアガイド</h3>
          <p class="resource-card__desc">
            就職活動の進め方、履歴書・職務経歴書の書き方
          </p>
        </div>
      </div>
      
      <div class="access-box">
        <h2 class="access-box__title">学習資産ハブにアクセス</h2>
        <p class="access-box__desc">
          会員の方は、以下のリンクから学習資産ハブにアクセスできます。<br />
          ※アクセスには会員登録が必要です。
        </p>
        <a 
          href={siteConfig.CANVA_RESOURCE_HUB_URL} 
          class="btn btn--primary btn--lg"
          target="_blank"
          rel="noopener noreferrer"
        >
          学習資産ハブを開く
        </a>
        
        <p class="access-box__note">
          まだ会員でない方は<a href="/join">こちらから入会</a>できます。
        </p>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .resources-hero {
    padding: var(--space-16) 0;
    background: linear-gradient(135deg, var(--color-accent) 0%, var(--color-accent-dark) 100%);
    color: white;
  }
  
  .resources-hero__title {
    font-size: var(--text-4xl);
    color: white;
    margin-bottom: var(--space-4);
  }
  
  .resources-hero__subtitle {
    font-size: var(--text-xl);
    opacity: 0.9;
  }
  
  .resources-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    margin-bottom: var(--space-16);
  }
  
  .resource-card {
    background: white;
    border-radius: var(--radius-lg);
    padding: var(--space-6);
    box-shadow: var(--shadow-md);
    text-align: center;
    transition: all var(--transition-normal);
  }
  
  .resource-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }
  
  .resource-card__icon {
    font-size: 3rem;
    display: block;
    margin-bottom: var(--space-4);
  }
  
  .resource-card__title {
    font-size: var(--text-lg);
    margin-bottom: var(--space-2);
  }
  
  .resource-card__desc {
    font-size: var(--text-sm);
    color: var(--color-text-light);
    margin: 0;
  }
  
  .access-box {
    background: var(--color-bg-alt);
    border-radius: var(--radius-xl);
    padding: var(--space-10);
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
  }
  
  .access-box__title {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-4);
  }
  
  .access-box__desc {
    color: var(--color-text-light);
    margin-bottom: var(--space-6);
  }
  
  .access-box__note {
    margin-top: var(--space-6);
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }
  
  @media (max-width: 1024px) {
    .resources-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (max-width: 640px) {
    .resources-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
