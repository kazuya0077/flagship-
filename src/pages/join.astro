---
import BaseLayout from '../layouts/BaseLayout.astro';
import { siteConfig } from '../config/site';

const steps = [
  {
    number: 1,
    title: "下の「今すぐ始める」から決済",
    description: "Stripeの安全な決済ページで月額500円のサブスクリプションに登録します。"
  },
  {
    number: 2,
    title: "メールでSlack招待リンクを受け取る",
    description: "決済完了後1日以内にSlackコミュニティへの招待リンクをメールでお送りします。"
  },
  {
    number: 3,
    title: "Slackに参加して自己紹介",
    description: "コミュニティに参加したら、まずは自己紹介チャンネルで挨拶しましょう！"
  },
  {
    number: 4,
    title: "学習資産にアクセス",
    description: "Canvaの学習テンプレート、過去問分析シートなどすべての資料にアクセスできます。"
  }
];

const includes = [
  { icon: "💬", title: "Slackコミュニティ", description: "質問・相談・雑談チャンネル" },
  { icon: "📚", title: "学習テンプレート", description: "年間計画、週間スケジュール等" },
  { icon: "📝", title: "要点まとめ資料", description: "科目別の重要ポイント" },
  { icon: "📊", title: "過去問分析シート", description: "頻出分野の整理・分析" },
  { icon: "🎓", title: "月2回のイベント", description: "勉強会・Q&Aセッション" },
  { icon: "🏥", title: "実習対策サポート", description: "レポート・コミュニケーション相談" },
];
---

<BaseLayout 
  title="入会する"
  description="PT国試コミュニティへの入会方法。月額500円で質問し放題、学習テンプレート、仲間との交流が手に入ります。"
>
  <!-- ヒーロー -->
  <section class="join-hero">
    <div class="container">
      <p class="join-hero__label">JOIN US</p>
      <h1 class="join-hero__title">今すぐ始めよう</h1>
      <p class="join-hero__subtitle">月額500円で、あなたの国試対策が変わります</p>
    </div>
  </section>

  <!-- 料金プラン -->
  <section class="section">
    <div class="container">
      <div class="pricing-card">
        <div class="pricing-card__header">
          <span class="pricing-card__badge">おすすめ</span>
          <h2 class="pricing-card__title">スタンダードプラン</h2>
          <div class="pricing-card__price">
            <span class="pricing-card__currency">¥</span>
            <span class="pricing-card__amount">500</span>
            <span class="pricing-card__period">/月（税込）</span>
          </div>
          <p class="pricing-card__note">いつでも解約OK</p>
        </div>
        
        <div class="pricing-card__content">
          <h3 class="pricing-card__includes-title">含まれるもの</h3>
          <div class="pricing-card__includes">
            {includes.map((item) => (
              <div class="include-item">
                <span class="include-item__icon">{item.icon}</span>
                <div>
                  <p class="include-item__title">{item.title}</p>
                  <p class="include-item__desc">{item.description}</p>
                </div>
              </div>
            ))}
          </div>
          
          <a href={siteConfig.links.stripeSubscription} class="btn btn--primary btn--lg pricing-card__cta" target="_blank" rel="noopener noreferrer">
            今すぐ始める
          </a>
          
          <p class="pricing-card__secure">
            🔒 Stripeによる安全な決済
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- 入会後の流れ -->
  <section class="section section--warm">
    <div class="container">
      <div class="section__header">
        <p class="section__label">Flow</p>
        <h2 class="section__title">入会後の流れ</h2>
      </div>
      
      <div class="steps">
        {steps.map((step, index) => (
          <div class="step-item">
            <div class="step-item__number">{step.number}</div>
            <div class="step-item__content">
              <h3 class="step-item__title">{step.title}</h3>
              <p class="step-item__description">{step.description}</p>
            </div>
            {index < steps.length - 1 && <div class="step-item__line"></div>}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- 解約について -->
  <section class="section section--alt">
    <div class="container container--narrow">
      <div class="section__header">
        <p class="section__label">Cancellation</p>
        <h2 class="section__title">解約について</h2>
      </div>
      
      <div class="card">
        <div class="cancel-info">
          <div class="cancel-info__item">
            <span class="cancel-info__icon">✓</span>
            <p><strong>いつでも解約可能</strong> - Stripeの管理画面から簡単に手続きできます</p>
          </div>
          <div class="cancel-info__item">
            <span class="cancel-info__icon">✓</span>
            <p><strong>解約後も当月末まで利用可能</strong> - 次回請求日まではサービスを継続できます</p>
          </div>
          <div class="cancel-info__item">
            <span class="cancel-info__icon">✓</span>
            <p><strong>違約金・手数料なし</strong> - 解約による追加費用は一切ありません</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 注意事項 -->
  <section class="section">
    <div class="container container--narrow">
      <div class="section__header">
        <h2 class="section__title">ご利用にあたって</h2>
      </div>
      
      <div class="notice-list">
        <div class="notice-item">
          <p>本サービスは<strong>合格を保証するものではありません</strong>。学習成果は個人の努力により異なります。</p>
        </div>
        <div class="notice-item">
          <p>提供される情報は参考目的であり、公式教材や教科書に代わるものではありません。</p>
        </div>
        <div class="notice-item">
          <p>コミュニティ内では互いを尊重したコミュニケーションをお願いします。</p>
        </div>
      </div>
      
      <div class="text-center mt-8">
        <a href="/terms" class="btn btn--secondary">利用規約を確認する</a>
      </div>
    </div>
  </section>

  <!-- 最終CTA -->
  <section class="cta-section">
    <div class="container cta-section__content">
      <h2 class="cta-section__title">まずは1ヶ月試してみませんか？</h2>
      <p class="cta-section__subtitle">
        いつでも解約OK。リスクなく始められます。
      </p>
      <a href={siteConfig.links.stripeSubscription} class="btn btn--primary btn--lg" target="_blank" rel="noopener noreferrer">
        今すぐ始める（月額500円）
      </a>
    </div>
  </section>
</BaseLayout>

<style>
  /* Join Hero */
  .join-hero {
    background: var(--gradient-hero);
    color: white;
    padding: calc(80px + var(--space-16)) 0 var(--space-16);
    text-align: center;
  }
  
  .join-hero__label {
    display: inline-block;
    padding: var(--space-2) var(--space-4);
    background: rgba(255, 255, 255, 0.2);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    margin-bottom: var(--space-4);
    backdrop-filter: blur(4px);
  }
  
  .join-hero__title {
    font-size: var(--text-4xl);
    color: white;
    margin-bottom: var(--space-4);
  }
  
  .join-hero__subtitle {
    font-size: var(--text-xl);
    opacity: 0.9;
  }
  
  /* Pricing Card */
  .pricing-card {
    max-width: 500px;
    margin: 0 auto;
    background: var(--color-bg-white);
    border-radius: var(--radius-2xl);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
  }
  
  .pricing-card__header {
    background: var(--gradient-primary);
    color: white;
    padding: var(--space-8);
    text-align: center;
    position: relative;
  }
  
  .pricing-card__badge {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    background: rgba(255, 255, 255, 0.25);
    border-radius: var(--radius-full);
    font-size: var(--text-xs);
    font-weight: var(--font-semibold);
    margin-bottom: var(--space-4);
  }
  
  .pricing-card__title {
    font-size: var(--text-xl);
    color: white;
    margin-bottom: var(--space-4);
  }
  
  .pricing-card__price {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: var(--space-1);
  }
  
  .pricing-card__currency {
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
  }
  
  .pricing-card__amount {
    font-size: var(--text-6xl);
    font-weight: var(--font-bold);
    line-height: 1;
    font-family: 'Inter', sans-serif;
  }
  
  .pricing-card__period {
    font-size: var(--text-lg);
    opacity: 0.9;
  }
  
  .pricing-card__note {
    margin-top: var(--space-2);
    font-size: var(--text-sm);
    opacity: 0.8;
  }
  
  .pricing-card__content {
    padding: var(--space-8);
  }
  
  .pricing-card__includes-title {
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--space-4);
  }
  
  .pricing-card__includes {
    display: grid;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
  }
  
  .include-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
  }
  
  .include-item__icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }
  
  .include-item__title {
    font-weight: var(--font-semibold);
    font-size: var(--text-base);
    margin-bottom: var(--space-1);
  }
  
  .include-item__desc {
    font-size: var(--text-sm);
    color: var(--color-text-light);
  }
  
  .pricing-card__cta {
    width: 100%;
    margin-bottom: var(--space-4);
  }
  
  .pricing-card__secure {
    text-align: center;
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }
  
  /* Steps */
  .steps {
    max-width: 600px;
    margin: 0 auto;
  }
  
  .step-item {
    display: flex;
    gap: var(--space-6);
    position: relative;
    padding-bottom: var(--space-8);
  }
  
  .step-item__number {
    width: 48px;
    height: 48px;
    flex-shrink: 0;
    background: var(--gradient-primary);
    color: white;
    border-radius: var(--radius-full);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    font-family: 'Inter', sans-serif;
    position: relative;
    z-index: 1;
  }
  
  .step-item__content {
    flex: 1;
    padding-top: var(--space-2);
  }
  
  .step-item__title {
    font-size: var(--text-lg);
    margin-bottom: var(--space-2);
  }
  
  .step-item__description {
    color: var(--color-text-light);
    font-size: var(--text-base);
  }
  
  .step-item__line {
    position: absolute;
    left: 23px;
    top: 56px;
    bottom: 0;
    width: 2px;
    background: var(--color-border);
  }
  
  /* Container narrow */
  .container--narrow {
    max-width: 700px;
  }
  
  /* Cancel Info */
  .cancel-info {
    display: grid;
    gap: var(--space-4);
  }
  
  .cancel-info__item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
  }
  
  .cancel-info__icon {
    width: 24px;
    height: 24px;
    flex-shrink: 0;
    background: var(--color-primary);
    color: white;
    border-radius: var(--radius-full);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--text-sm);
    font-weight: var(--font-bold);
  }
  
  .cancel-info__item p {
    margin: 0;
    color: var(--color-text-light);
  }
  
  /* Notice */
  .notice-list {
    display: grid;
    gap: var(--space-4);
  }
  
  .notice-item {
    padding: var(--space-4);
    background: var(--color-bg-alt);
    border-radius: var(--radius-lg);
    border-left: 3px solid var(--color-secondary);
  }
  
  .notice-item p {
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text-light);
  }
</style>
