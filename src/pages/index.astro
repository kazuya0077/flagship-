---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout>

  <!-- ══════════════════════════════════════════
       HERO — FLAGSHIP SVG Path Tracing
  ══════════════════════════════════════════ -->
  <section class="hero" id="hero">
    <div class="hero__bg" aria-hidden="true"></div>
    <div class="hero__noise" aria-hidden="true"></div>

    <div class="hero__content">
      <p class="hero__kicker">// PT COMMUNITY PLATFORM</p>

      <!-- SVG path tracing animation -->
      <div class="hero__svg-wrap" aria-label="FLAGSHIP">
        <svg class="hero__svg" viewBox="0 0 900 120" preserveAspectRatio="xMidYMid meet">
          <text
            class="flagship-trace"
            x="50%"
            y="88%"
            text-anchor="middle"
            font-family="Inter, sans-serif"
            font-weight="900"
            font-size="108"
            fill="transparent"
            stroke="#00F0FF"
            stroke-width="1"
            letter-spacing="-4"
          >FLAGSHIP</text>
        </svg>
      </div>

      <p class="hero__sub">
        理学療法学生の不安・孤独をなくし、<span class="pc-br"><br></span>
        学習・臨床・キャリアをつなげるコミュニティ
      </p>

      <div class="hero__actions">
        <a href="/#join"  class="btn btn--primary">JOIN FREE →</a>
        <a href="/#about" class="btn btn--outline">ABOUT US</a>
      </div>
    </div>

    <!-- scroll indicator -->
    <div class="hero__scroll" aria-hidden="true">
      <span class="hero__scroll-label">SCROLL</span>
      <div class="hero__scroll-bar"></div>
    </div>
  </section>


  <!-- ══════════════════════════════════════════
       01 — ABOUT / MVV
  ══════════════════════════════════════════ -->
  <section id="about">
    <div class="container">

      <p class="sec-label reveal">// 01 &nbsp; ABOUT</p>
      <h2 class="sec-title reveal d1">
        なぜFLAGSHIPは<br>存在するのか
      </h2>
      <p class="sec-desc reveal d2">
        理学療法士を目指す学生が、真に自分の頭で考え、選択し、成長できる環境は
        これまでほとんど存在しなかった。FLAGSHIPはその空白を埋めるために生まれた。
      </p>

      <div class="mvv-grid">

        <!-- MISSION -->
        <article class="mvv-card glass target-card reveal d1">
          <div class="mvv-card__head">
            <span class="mvv-card__num">01</span>
            <span class="mvv-card__badge">MISSION</span>
          </div>
          <h3 class="mvv-card__title">Mission</h3>
          <div class="mvv-card__line"></div>
          <p class="mvv-card__text">
            理学療法学生の<strong>不安・孤独をなくし</strong>、
            学習・臨床・キャリアをつなげて理解できる環境を提供し人材を育成する
          </p>
        </article>

        <!-- VISION -->
        <article class="mvv-card glass target-card reveal d2">
          <div class="mvv-card__head">
            <span class="mvv-card__num">02</span>
            <span class="mvv-card__badge">VISION</span>
          </div>
          <h3 class="mvv-card__title">Vision</h3>
          <div class="mvv-card__line"></div>
          <p class="mvv-card__text">
            <strong>自分で考え、選び、成長できる理学療法士</strong>が当たり前の世界・業界
          </p>
          <ul class="mvv-card__bullets">
            <li>国試後に燃え尽きない PT を増やす</li>
            <li>「とりあえず急性期」の慣例を変える</li>
            <li>SNS情報に振り回されない自分軸を育てる</li>
          </ul>
        </article>

        <!-- VALUES -->
        <article class="mvv-card glass target-card reveal d3">
          <div class="mvv-card__head">
            <span class="mvv-card__num">03</span>
            <span class="mvv-card__badge">VALUES</span>
          </div>
          <h3 class="mvv-card__title">Values</h3>
          <div class="mvv-card__line"></div>
          <ul class="mvv-card__values">
            <li><span class="v-dash">—</span>正解より、思考・試行を大切にする</li>
            <li><span class="v-dash">—</span>比較ではなく、成長に目を向ける</li>
            <li><span class="v-dash">—</span>社会で使える自己理解を目指す</li>
            <li><span class="v-dash">—</span>挑戦を応援する</li>
          </ul>
        </article>

      </div><!-- /mvv-grid -->

    </div>
  </section>


  <!-- ══════════════════════════════════════════
       02 — SERVICE
  ══════════════════════════════════════════ -->
  <section id="service" class="sec-alt">
    <div class="container">

      <p class="sec-label reveal">// 02 &nbsp; SERVICE</p>
      <h2 class="sec-title reveal d1">2つの学習トラック</h2>
      <p class="sec-desc reveal d2">
        入学直後から卒業後まで。あなたのフェーズに合わせた
        学習とキャリア形成を、コミュニティが伴走します。
      </p>

      <div class="service-grid">

        <!-- Track 01 -->
        <article class="service-card glass target-card reveal d1">
          <div class="service-card__num">01</div>
          <h3 class="service-card__title">国試・学業サポート</h3>
          <p class="service-card__en">LICENSING EXAM SUPPORT</p>
          <p class="service-card__desc">
            効率的な国試対策と日常の学習を、コミュニティの力で乗り越える。
            一人で抱えず、仲間と一緒に前へ進む。
          </p>
          <ul class="service-card__list">
            <li>国試勉強の進め方・戦略設計</li>
            <li>国試対策ミニ講義コンテンツ</li>
            <li>領域別「ここだけは押さえる」要点整理</li>
            <li>授業・実習の質問OKチャンネル</li>
          </ul>
        </article>

        <!-- Track 02 -->
        <article class="service-card glass target-card reveal d2">
          <div class="service-card__num">02</div>
          <h3 class="service-card__title">キャリア・価値観形成</h3>
          <p class="service-card__en">CAREER DEVELOPMENT</p>
          <p class="service-card__desc">
            入学直後のギャップから、将来の不安まで。
            言語化し、仲間と整理し、自分の軸を育てる場所。
          </p>
          <ul class="service-card__list">
            <li>月1テーマ型座談会（音声 / Zoom）</li>
            <li>「失敗談・遠回り談」先輩からのリアル</li>
            <li>キャリアの選択肢マップ</li>
            <li>卒業後1〜5年目のリアル解説</li>
          </ul>
        </article>

      </div><!-- /service-grid -->

    </div>
  </section>


  <!-- ══════════════════════════════════════════
       03 — COMMUNITY FEATURES
  ══════════════════════════════════════════ -->
  <section id="community">
    <div class="container">

      <p class="sec-label reveal">// 03 &nbsp; COMMUNITY</p>
      <h2 class="sec-title reveal d1">
        コミュニティで<br>得られるもの
      </h2>

      <div class="features-grid">

        <article class="feature-card glass target-card reveal d1">
          <div class="feature-card__icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M9 3H5a2 2 0 00-2 2v4m6-6h10a2 2 0 012 2v4M9 3v18m0 0h10a2 2 0 002-2V9M9 21H5a2 2 0 01-2-2V9m0 0h18"/>
            </svg>
          </div>
          <h3 class="feature-card__title">学習メソッド</h3>
          <p>正解を暗記するのではなく、<strong>考える力を育てる</strong>学習アプローチを身につける。</p>
        </article>

        <article class="feature-card glass target-card reveal d2">
          <div class="feature-card__icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
          </div>
          <h3 class="feature-card__title">相互サポート</h3>
          <p>不安を言語化し、先輩・仲間からのフィードバックで<strong>思考を整理</strong>する場。</p>
        </article>

        <article class="feature-card glass target-card reveal d3">
          <div class="feature-card__icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
            </svg>
          </div>
          <h3 class="feature-card__title">キャリア設計</h3>
          <p>「とりあえず急性期」ではなく、<strong>自分が選んだ道</strong>を歩める判断力を養う。</p>
        </article>

        <article class="feature-card glass target-card reveal d4">
          <div class="feature-card__icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
          </div>
          <h3 class="feature-card__title">先輩ネットワーク</h3>
          <p>卒業後1〜5年目のリアルを知り、<strong>未来から逆算</strong>して学生生活を設計する。</p>
        </article>

      </div><!-- /features-grid -->

      <!-- Purpose quote -->
      <blockquote class="purpose-quote reveal">
        <p class="purpose-quote__text">
          "学生生活と国家試験のその先を見据え、<br class="pc-br">
          理学療法士の学問を修めた人間として<br class="pc-br">
          自分の軸を持って働ける人を増やす。"
        </p>
        <cite class="purpose-quote__cite">— FLAGSHIP PURPOSE</cite>
      </blockquote>

    </div>
  </section>


  <!-- ══════════════════════════════════════════
       04 — CTA / JOIN
  ══════════════════════════════════════════ -->
  <section id="join" class="sec-cta">
    <div class="container">
      <div class="cta-box glass">
        <div class="cta-box__bg-text" aria-hidden="true">FLAGSHIP</div>

        <div class="cta-box__inner">
          <p class="sec-label reveal">// JOIN US</p>
          <h2 class="cta-box__title reveal d1">
            さあ、<br>自分の軸を作れ。
          </h2>
          <p class="cta-box__desc reveal d2">
            入学直後でも、国試直前でも、卒業後でも。<br class="pc-br">
            どのフェーズにいるあなたにも、FLAGSHIPは開かれています。
          </p>

          <div class="cta-box__actions reveal d3">
            <a
              href="https://join.slack.com/placeholder"
              target="_blank"
              rel="noopener noreferrer"
              class="btn btn--primary cta-box__btn"
            >
              無料でコミュニティに参加する →
            </a>
          </div>

          <ul class="cta-box__notes reveal d4">
            <li>// 参加は完全無料</li>
            <li>// 学生・入学直後からOK</li>
            <li>// いつでも退会可能</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
/* ════════════════════════════════════════
   HERO
════════════════════════════════════════ */
.hero {
  position: relative;
  height: 100svh;
  min-height: 620px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.hero__bg {
  position: absolute;
  inset: 0;
  background-image: url('https://static.vecteezy.com/system/resources/previews/021/039/790/non_2x/abstract-technology-futuristic-glowing-neon-blue-and-pink-light-lines-with-speed-motion-movingon-dark-blue-background-vector.jpg');
  background-size: cover;
  background-position: center;
  opacity: 0.28;
}

.hero__noise {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to bottom,
    rgba(5,5,5,0.3) 0%,
    rgba(5,5,5,0.1) 40%,
    rgba(5,5,5,0.6) 80%,
    rgba(5,5,5,1)   100%
  );
}

.hero__content {
  position: relative;
  z-index: 2;
  text-align: center;
  padding: 0 24px;
  width: 100%;
  max-width: 960px;
}

/* kicker */
.hero__kicker {
  font-family: 'Inter', sans-serif;
  font-size: 0.65rem;
  font-weight: 700;
  letter-spacing: 0.35em;
  color: var(--accent);
  margin-bottom: 2.5rem;
  opacity: 0;
  animation: fadeIn 0.8s var(--ease) 0.2s forwards;
}

/* SVG text */
.hero__svg-wrap {
  width: 100%;
  max-width: 840px;
  margin: 0 auto 2.5rem;
}

.hero__svg {
  width: 100%;
  height: auto;
  overflow: visible;
}

/* Phase 1: draw outline (0.6s → 2.1s)
   Phase 2: fill white  (2.1s → 2.6s)
   Phase 3: logo slides up (2.8s → 3.4s) via parent */
.flagship-trace {
  stroke-dasharray: 6000;
  stroke-dashoffset: 6000;
  animation:
    drawOutline 1.5s cubic-bezier(0.16, 1, 0.3, 1) 0.6s forwards,
    fillWhite   0.5s ease 2.1s forwards,
    fadeStroke  0.5s ease 2.1s forwards;
}

@keyframes drawOutline {
  to { stroke-dashoffset: 0; }
}
@keyframes fillWhite {
  to { fill: #ffffff; }
}
@keyframes fadeStroke {
  to { stroke: transparent; }
}

/* Phase 3: SVG wrap slides up and fades */
.hero__svg-wrap {
  animation: logoSlideUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 2.8s both;
}

@keyframes logoSlideUp {
  from { opacity: 1; transform: translateY(0); }
  to   { opacity: 0; transform: translateY(-40px); }
}

/* Phase 3: sub + actions fade in after logo slides out */
.hero__sub {
  font-size: clamp(0.88rem, 2vw, 1.05rem);
  color: #A0A0A0;
  line-height: 1.9;
  margin-bottom: 2.8rem;
  opacity: 0;
  animation: fadeUp 1.0s cubic-bezier(0.16, 1, 0.3, 1) 3.3s forwards;
}

.hero__actions {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
  opacity: 0;
  animation: fadeUp 1.0s cubic-bezier(0.16, 1, 0.3, 1) 3.45s forwards;
}

/* scroll hint */
.hero__scroll {
  position: absolute;
  bottom: 36px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  opacity: 0;
  animation: fadeIn 1s ease 3.8s forwards;
}
.hero__scroll-label {
  font-family: 'Inter', sans-serif;
  font-size: 0.58rem;
  letter-spacing: 0.35em;
  color: var(--text-dim);
}
.hero__scroll-bar {
  width: 1px;
  height: 44px;
  background: linear-gradient(to bottom, var(--accent), transparent);
  animation: scrollPulse 2s ease-in-out 4s infinite;
}

@keyframes scrollPulse {
  0%, 100% { opacity: 1; transform: scaleY(1); }
  50%       { opacity: 0.4; transform: scaleY(0.6); }
}
@keyframes fadeIn { to { opacity: 1; } }
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(18px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* ════════════════════════════════════════
   MVV GRID
════════════════════════════════════════ */
.mvv-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1px;
  background: rgba(0,240,255,0.08);
}

.mvv-card {
  padding: 3rem 2.5rem;
  background: var(--bg);
  transition: background 0.3s;
}
.mvv-card:hover { background: rgba(0,240,255,0.03); }

.mvv-card__head {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 1.5rem;
}
.mvv-card__num {
  font-family: 'Inter', sans-serif;
  font-weight: 900;
  font-size: 2.2rem;
  color: rgba(0,240,255,0.12);
  line-height: 1;
}
.mvv-card__badge {
  font-family: 'Inter', sans-serif;
  font-size: 0.58rem;
  font-weight: 700;
  letter-spacing: 0.22em;
  color: var(--accent);
  border: 1px solid var(--accent);
  padding: 3px 9px;
}
.mvv-card__title {
  font-size: 1.3rem;
  color: #fff;
  margin-bottom: 0.8rem;
}
.mvv-card__line {
  width: 28px;
  height: 1px;
  background: var(--accent);
  margin-bottom: 1.2rem;
}
.mvv-card__text {
  color: var(--text-mid);
  font-size: 0.9rem;
  line-height: 1.85;
}
.mvv-card__text strong { color: var(--text); }

.mvv-card__bullets {
  list-style: none;
  margin-top: 1.2rem;
  display: flex;
  flex-direction: column;
  gap: 0.6rem;
}
.mvv-card__bullets li {
  font-size: 0.85rem;
  color: var(--text-dim);
  padding-left: 0;
  border-top: 1px solid rgba(255,255,255,0.05);
  padding-top: 0.6rem;
}
.mvv-card__bullets li:first-child { border-top: none; padding-top: 0; }

.mvv-card__values {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}
.mvv-card__values li {
  display: flex;
  gap: 0.75rem;
  font-size: 0.88rem;
  color: var(--text-mid);
  line-height: 1.6;
}
.v-dash { color: var(--accent); font-weight: 700; flex-shrink: 0; }

/* ════════════════════════════════════════
   SEC-ALT
════════════════════════════════════════ */
.sec-alt {
  background: rgba(0,240,255,0.015);
  border-top: 1px solid rgba(0,240,255,0.1);
  border-bottom: 1px solid rgba(0,240,255,0.1);
}

/* ════════════════════════════════════════
   SERVICE GRID
════════════════════════════════════════ */
.service-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1px;
  background: rgba(0,240,255,0.08);
}

.service-card {
  padding: 3.5rem 3rem;
  background: var(--bg);
  transition: background 0.3s;
}
.service-card:hover { background: rgba(0,240,255,0.025); }

.service-card__num {
  font-family: 'Inter', sans-serif;
  font-weight: 900;
  font-size: 4rem;
  color: rgba(0,240,255,0.07);
  line-height: 1;
  margin-bottom: 1rem;
}
.service-card__title {
  font-size: 1.55rem;
  color: #fff;
  margin-bottom: 0.35rem;
}
.service-card__en {
  font-family: 'Inter', sans-serif;
  font-size: 0.62rem;
  font-weight: 700;
  letter-spacing: 0.22em;
  color: var(--accent);
  margin-bottom: 1.5rem;
}
.service-card__desc {
  color: var(--text-mid);
  font-size: 0.9rem;
  line-height: 1.85;
  padding-bottom: 1.8rem;
  margin-bottom: 1.8rem;
  border-bottom: 1px solid rgba(255,255,255,0.06);
}
.service-card__list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}
.service-card__list li {
  color: var(--text-dim);
  font-size: 0.88rem;
  padding-left: 1.2rem;
  position: relative;
}
.service-card__list li::before {
  content: '→';
  position: absolute;
  left: 0;
  color: var(--accent);
  font-family: 'Inter', sans-serif;
  font-size: 0.75rem;
}

/* ════════════════════════════════════════
   FEATURES GRID
════════════════════════════════════════ */
.features-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1px;
  background: rgba(0,240,255,0.08);
  margin-bottom: 6rem;
}

.feature-card {
  padding: 3rem 2.5rem;
  background: var(--bg);
  transition: background 0.3s;
}
.feature-card:hover { background: rgba(0,240,255,0.025); }

.feature-card__icon {
  color: var(--accent);
  margin-bottom: 1.5rem;
}
.feature-card__title {
  font-size: 1.15rem;
  color: #fff;
  margin-bottom: 0.8rem;
}
.feature-card p {
  color: var(--text-mid);
  font-size: 0.88rem;
  line-height: 1.85;
}
.feature-card p strong { color: var(--text); }

/* PURPOSE QUOTE */
.purpose-quote {
  border-left: 2px solid var(--accent);
  padding: 2rem 2.5rem;
  background: rgba(0,240,255,0.03);
}
.purpose-quote__text {
  font-family: 'Inter', 'Noto Sans JP', sans-serif;
  font-size: clamp(1rem, 2.5vw, 1.3rem);
  font-weight: 700;
  color: rgba(224,224,224,0.8);
  line-height: 1.8;
  margin-bottom: 1rem;
}
.purpose-quote__cite {
  font-family: 'Inter', sans-serif;
  font-size: 0.65rem;
  letter-spacing: 0.25em;
  color: var(--accent);
  font-style: normal;
}

/* ════════════════════════════════════════
   CTA
════════════════════════════════════════ */
.sec-cta {
  border-top: 1px solid rgba(0,240,255,0.1);
}

.cta-box {
  border: 1px solid rgba(0,240,255,0.18);
  position: relative;
  overflow: hidden;
}

.cta-box__bg-text {
  position: absolute;
  font-family: 'Inter', sans-serif;
  font-weight: 900;
  font-size: clamp(5rem, 14vw, 12rem);
  color: rgba(0,240,255,0.025);
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  white-space: nowrap;
  pointer-events: none;
  user-select: none;
  letter-spacing: -0.04em;
}

.cta-box__inner {
  position: relative;
  z-index: 1;
  padding: 6rem 5rem;
  text-align: center;
}

.cta-box__title {
  font-size: clamp(2.5rem, 6vw, 4.5rem);
  color: #fff;
  margin-bottom: 1.5rem;
}

.cta-box__desc {
  color: var(--text-mid);
  font-size: 1rem;
  line-height: 1.9;
  margin-bottom: 3rem;
}

.cta-box__actions { margin-bottom: 2.5rem; }

.cta-box__btn { padding: 16px 44px; font-size: 0.78rem; }

.cta-box__notes {
  list-style: none;
  display: flex;
  gap: 2rem;
  justify-content: center;
  flex-wrap: wrap;
}
.cta-box__notes li {
  font-family: 'Inter', sans-serif;
  font-size: 0.65rem;
  letter-spacing: 0.12em;
  color: rgba(0,240,255,0.5);
}

/* ════════════════════════════════════════
   RESPONSIVE
════════════════════════════════════════ */
@media (max-width: 900px) {
  .mvv-grid      { grid-template-columns: 1fr; }
  .service-grid  { grid-template-columns: 1fr; }
  .features-grid { grid-template-columns: 1fr; }
  .cta-box__inner { padding: 4rem 2rem; }
}

@media (max-width: 600px) {
  .mvv-card    { padding: 2.5rem 1.5rem; }
  .service-card { padding: 2.5rem 1.5rem; }
  .feature-card { padding: 2rem 1.5rem; }
  .hero__svg-wrap { max-width: 100%; }
}
</style>
