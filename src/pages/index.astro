---
import BaseLayout from '../layouts/BaseLayout.astro';
import AspLink from '../components/AspLink.astro';
import { siteConfig } from '../config/site';

const features = [
  {
    title: "完全無料で参加",
    desc: "入会費・月会費一切なし。Slackに参加するだけで、すべての学習コンテンツが無料で使えます。",
    wide: true,
    icon: "🎁",
    color: "#FFF3E0",
  },
  {
    title: "Slackで質問し放題",
    desc: "24時間365日、いつでも質問OK。国試高得点合格者が丁寧に回答します。",
    wide: false,
    icon: "💬",
    color: "#E3F2FD",
  },
  {
    title: "学習資産を無料DL",
    desc: "合格者のまとめノート・分析シートをすべて無料でダウンロードできます。",
    wide: false,
    icon: "📁",
    color: "#E8F5E9",
  },
  {
    title: "仲間との繋がり",
    desc: "同じ目標を持つPT学生と励まし合い、一緒に国試合格を目指せます。",
    wide: false,
    icon: "👥",
    color: "#FCE4EC",
  },
  {
    title: "月2回のオンライン勉強会",
    desc: "国試対策の疑問をリアルタイムで解消。直接アドバイスが受けられます。",
    wide: false,
    icon: "📖",
    color: "#F3E5F5",
  },
];

const stats = [
  { label: "講師国試得点", value: "238", unit: "点" },
  { label: "学習資料数", value: "50", unit: "+" },
  { label: "回答時間", value: "24", unit: "h以内" },
  { label: "継続率", value: "98", unit: "%" },
];

const examCategories = [
  { name: "解剖学・生理学", icon: "🧬", href: "/resources" },
  { name: "運動学", icon: "🦴", href: "/resources" },
  { name: "内部障害", icon: "❤️", href: "/resources" },
  { name: "神経疾患", icon: "🧠", href: "/resources" },
  { name: "整形外科疾患", icon: "🦿", href: "/resources" },
  { name: "過去問・予想問", icon: "📝", href: "/resources" },
];

import { getRecommendItems } from '../library/microcms';
const items = await getRecommendItems();
---

<BaseLayout
  title="FLAGSHIP | PT国試対策・無料学習コミュニティ"
  description="理学療法士国家試験対策の情報サイト。おすすめ教材・学習法・無料Slackコミュニティを提供。合格者運営。"
>
  <!-- Opening Animation -->
  <div class="opening-overlay">
    <div class="opening-logo">
      <img src="/assets/images/flagship-logo.png" alt="FLAGSHIP" class="opening-img" onerror="this.style.display='none'; this.nextElementSibling.style.display='block'" />
      <span class="opening-text-fallback" style="display:none">FLAGSHIP</span>
    </div>
  </div>

  <!-- ========== HERO ========== -->
  <section class="hero">
    <div class="container hero__container">
      <div class="hero__content scroll-fade-up">
        <span class="hero__tag">PT国試対策 × 無料Slackコミュニティ</span>
        <h1 class="hero__title">
          自走する力を、<br>
          <span class="text-primary">ここ</span>から。
        </h1>
        <p class="hero__lead">
          理学療法士国家試験対策の情報を、無料で。<br>
          おすすめ教材・効率的な学習法の発信と、<br>
          合格者と繋がれる<strong>無料Slackコミュニティ</strong>を提供。
        </p>
        <div class="hero__actions">
          <a href={siteConfig.links.slackJoin} target="_blank" rel="noopener" class="btn btn--primary btn--lg">
            無料でSlackに参加する →
          </a>
          <a href="#books" class="btn btn--outline">
            おすすめ教材を見る
          </a>
        </div>
        <ul class="hero__badges">
          <li>✓ 完全無料</li>
          <li>✓ Slack活用</li>
          <li>✓ 国試合格者運営</li>
        </ul>
      </div>
      <div class="hero__image scroll-fade-up delay-1">
        <img
          src="/assets/images/flagship-logo.png"
          alt="FLAGSHIP - PT国試対策コミュニティ"
          class="hero__logo-img"
        />
      </div>
    </div>
  </section>

  <!-- ========== BANNER AD TOP (728×90) ========== -->
  <div class="banner-zone">
    <div class="container">
      <p class="banner-label">広告</p>
      <div class="banner-ad banner-ad--leaderboard">
        <!-- ここに広告タグを挿入してください -->
        <div class="banner-placeholder">広告スペース (728×90)</div>
      </div>
    </div>
  </div>

  <!-- ========== EXAM CATEGORIES ========== -->
  <section id="categories" class="section">
    <div class="container">
      <span class="section-label">CATEGORIES</span>
      <h2 class="section-title scroll-fade-up">国試対策カテゴリ</h2>
      <div class="category-grid">
        {examCategories.map(cat => (
          <a href={cat.href} class="category-card scroll-fade-up">
            <span class="category-icon">{cat.icon}</span>
            <span class="category-name">{cat.name}</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ========== ABOUT ========== -->
  <section id="about" class="section section--light">
    <div class="container about-inner">
      <div class="about-image scroll-fade-up">
        <img src="/assets/images/flagship-logo.png" alt="FLAGSHIP" />
      </div>
      <div class="about-content scroll-fade-up delay-1">
        <span class="section-label" style="text-align:left">ABOUT</span>
        <h2 class="about-title">FLAGSHIPとは？</h2>
        <p>
          FLAGSHIPは、理学療法士国家試験対策を頑張るすべての学生のためのサイトです。
        </p>
        <p>
          国試で高得点を取った運営者が実体験をもとに、おすすめ教材・効率的な学習法を発信。
          さらに<strong>無料のSlackコミュニティ</strong>で、仲間と一緒に合格を目指せます。
        </p>
        <a href={siteConfig.links.slackJoin} target="_blank" rel="noopener" class="btn btn--primary">
          無料コミュニティに参加する →
        </a>
      </div>
    </div>
  </section>

  <!-- ========== COMMUNITY FEATURES ========== -->
  <section id="features" class="section">
    <div class="container">
      <span class="section-label">COMMUNITY</span>
      <h2 class="section-title scroll-fade-up">無料コミュニティの特徴</h2>

      <!-- PC: Bento Grid -->
      <div class="bento-grid pc-only">
        {features.map(f => (
          <div
            class={`bento-item scroll-fade-up ${f.wide ? 'col-span-2' : 'col-span-1'} row-span-1`}
            style={`background: ${f.color}; box-shadow: none; border: 1px solid rgba(0,0,0,0.06);`}
          >
            <div class="bento-content">
              <span class="bento-icon">{f.icon}</span>
              <h3 class="bento-title">{f.title}</h3>
              <p class="bento-desc">{f.desc}</p>
            </div>
          </div>
        ))}
      </div>

      <!-- SP: List -->
      <div class="feature-list sp-only">
        {features.map(f => (
          <div class="feature-item scroll-fade-up" style={`background: ${f.color};`}>
            <span class="feature-icon">{f.icon}</span>
            <div>
              <h3 class="feature-title">{f.title}</h3>
              <p class="feature-desc">{f.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ========== STATS ========== -->
  <section class="section section--accent">
    <div class="container">
      <div class="stats-grid">
        {stats.map(stat => (
          <div class="stat-item scroll-fade-up">
            <div class="stat-value">{stat.value}<span class="stat-unit">{stat.unit}</span></div>
            <div class="stat-label">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ========== BANNER AD MID (300×250 ×2) ========== -->
  <div class="banner-zone">
    <div class="container">
      <p class="banner-label">広告</p>
      <div class="banner-mid-grid">
        <div class="banner-ad banner-ad--rect">
          <!-- 300×250 広告タグ -->
          <div class="banner-placeholder">広告スペース (300×250)</div>
        </div>
        <div class="banner-ad banner-ad--rect">
          <!-- 300×250 広告タグ -->
          <div class="banner-placeholder">広告スペース (300×250)</div>
        </div>
      </div>
    </div>
  </div>

  <!-- ========== RECOMMENDED BOOKS (ASP) ========== -->
  <section id="books" class="section section--light">
    <div class="container">
      <span class="section-label">RECOMMEND</span>
      <h2 class="section-title scroll-fade-up">おすすめ教材</h2>
      <p class="section-sub scroll-fade-up">国試高得点合格者が実際に使った教材を厳選紹介。</p>
      <div class="asp-grid">
        {items.map(item => (
          <AspLink
            title={item.title}
            description={item.description}
            links={item.links}
            image={item.image}
          />
        ))}
      </div>
    </div>
  </section>

  <!-- ========== SLACK CTA ========== -->
  <section class="slack-cta">
    <div class="container">
      <div class="slack-cta__inner scroll-fade-up">
        <div class="slack-cta__image">
          <img src="/assets/images/flagship-logo.png" alt="FLAGSHIP コミュニティ" />
        </div>
        <div class="slack-cta__content">
          <h2 class="slack-cta__title">
            無料Slackコミュニティで<br>
            一緒に合格を目指そう
          </h2>
          <ul class="slack-cta__list">
            <li>✓ 入会費・月会費：<strong>完全無料</strong></li>
            <li>✓ 国試合格者への質問し放題</li>
            <li>✓ 学習資料・まとめノートの無料DL</li>
            <li>✓ 月2回オンライン勉強会</li>
          </ul>
          <a href={siteConfig.links.slackJoin} target="_blank" rel="noopener" class="btn btn--primary btn--lg">
            Slackに無料参加する →
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== BANNER AD BOTTOM (728×90) ========== -->
  <div class="banner-zone banner-zone--bottom">
    <div class="container">
      <p class="banner-label">広告</p>
      <div class="banner-ad banner-ad--leaderboard">
        <!-- ここに広告タグを挿入してください -->
        <div class="banner-placeholder">広告スペース (728×90)</div>
      </div>
    </div>
  </div>

</BaseLayout>

<style>
  /* ========== HERO ========== */
  .hero {
    padding: 120px 0 80px;
    background: #fff;
  }

  .hero__container {
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
    align-items: center;
  }

  @media (min-width: 768px) {
    .hero__container {
      grid-template-columns: 1fr 1fr;
    }
    .hero {
      padding: 140px 0 100px;
    }
  }

  .hero__tag {
    display: inline-block;
    padding: 6px 14px;
    background: var(--primary);
    color: white;
    font-size: 0.78rem;
    font-weight: 700;
    border-radius: var(--radius-full);
    margin-bottom: 24px;
    letter-spacing: 0.05em;
  }

  .hero__title {
    font-size: 2.6rem;
    line-height: 1.25;
    margin-bottom: 24px;
    color: var(--text);
  }

  @media (min-width: 768px) {
    .hero__title { font-size: 3.6rem; }
  }

  .text-primary { color: var(--primary); }

  .hero__lead {
    font-size: 1rem;
    color: var(--text-light);
    margin-bottom: 36px;
    line-height: 1.9;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-bottom: 24px;
  }

  .btn--lg { padding: 16px 36px; font-size: 1rem; }

  .btn--outline {
    background: transparent;
    color: var(--primary);
    border: 2px solid var(--primary);
    border-radius: var(--radius-full);
    padding: 14px 32px;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.2s ease;
    display: inline-flex;
    align-items: center;
  }
  .btn--outline:hover { background: var(--primary); color: white; }

  .hero__badges {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    list-style: none;
    font-size: 0.85rem;
    color: var(--text-muted);
    font-weight: 600;
  }

  .hero__image {
    display: flex;
    justify-content: center;
  }

  .hero__logo-img {
    width: 100%;
    max-width: 480px;
    height: auto;
    filter: drop-shadow(0 8px 24px rgba(0,0,0,0.08));
  }

  /* ========== OPENING ========== */
  .opening-overlay {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background: white;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: fadeOut 0.4s ease 1.6s forwards;
    pointer-events: none;
  }

  .opening-logo {
    opacity: 0;
    animation: fadeInOut 1.6s ease forwards;
    text-align: center;
  }

  .opening-img {
    width: 200px;
    height: auto;
  }

  .opening-text-fallback {
    font-size: 2rem;
    font-weight: 800;
    color: var(--primary);
  }

  @keyframes fadeInOut {
    0%   { opacity: 0; transform: scale(0.95); }
    20%  { opacity: 1; transform: scale(1); }
    80%  { opacity: 1; transform: scale(1); }
    100% { opacity: 0; transform: scale(1.02); }
  }
  @keyframes fadeOut { to { opacity: 0; visibility: hidden; } }

  /* ========== BANNER AD ========== */
  .banner-zone {
    padding: 16px 0;
    background: #f9f9f9;
    border-top: 1px solid #eee;
    border-bottom: 1px solid #eee;
  }

  .banner-zone--bottom { margin-top: 0; }

  .banner-label {
    font-size: 0.7rem;
    color: #aaa;
    margin-bottom: 4px;
    text-align: center;
  }

  .banner-ad {
    display: flex;
    justify-content: center;
  }

  .banner-ad--leaderboard {
    max-width: 728px;
    width: 100%;
    margin: 0 auto;
  }

  .banner-mid-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    justify-content: center;
  }

  .banner-ad--rect {
    width: 300px;
  }

  .banner-placeholder {
    width: 100%;
    background: #e8e8e8;
    border: 2px dashed #ccc;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #999;
    font-size: 0.75rem;
    font-weight: 600;
    border-radius: 4px;
  }

  .banner-ad--leaderboard .banner-placeholder {
    height: 90px;
  }

  .banner-ad--rect .banner-placeholder {
    height: 250px;
  }

  /* ========== CATEGORIES ========== */
  .category-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }

  @media (min-width: 600px) {
    .category-grid { grid-template-columns: repeat(3, 1fr); }
  }

  @media (min-width: 900px) {
    .category-grid { grid-template-columns: repeat(6, 1fr); }
  }

  .category-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    padding: 24px 16px;
    background: #fff;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--text);
    font-weight: 700;
    font-size: 0.85rem;
    transition: all 0.2s ease;
    box-shadow: var(--shadow-sm);
    text-align: center;
  }

  .category-card:hover {
    border-color: var(--primary);
    background: #FFF8F5;
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .category-icon { font-size: 1.8rem; }
  .category-name { line-height: 1.4; }

  /* ========== ABOUT ========== */
  .section--light { background: #FAFAFA; }

  .about-inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
    align-items: center;
  }

  @media (min-width: 768px) {
    .about-inner { grid-template-columns: 1fr 1fr; }
  }

  .about-image img {
    width: 100%;
    max-width: 400px;
    height: auto;
    display: block;
    margin: 0 auto;
  }

  .about-title {
    font-size: 1.8rem;
    margin-bottom: 20px;
  }

  .about-content p {
    margin-bottom: 16px;
  }

  /* ========== BENTO GRID ICON ========== */
  .bento-icon {
    font-size: 2rem;
    display: block;
    margin-bottom: 12px;
  }

  /* ========== SP FEATURE LIST ========== */
  .feature-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .feature-item {
    display: flex;
    align-items: flex-start;
    gap: 16px;
    padding: 20px;
    border-radius: var(--radius-md);
    border: 1px solid rgba(0,0,0,0.05);
  }

  .feature-icon { font-size: 1.6rem; flex-shrink: 0; }

  .feature-title {
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 4px;
    color: var(--text);
  }

  .feature-desc {
    font-size: 0.85rem;
    color: var(--text-light);
    line-height: 1.6;
    margin: 0;
  }

  /* ========== STATS ========== */
  .section--accent {
    background: var(--primary);
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px;
  }

  @media (min-width: 768px) {
    .stats-grid { grid-template-columns: repeat(4, 1fr); }
  }

  .stat-item { text-align: center; }

  .stat-value {
    font-size: 2.5rem;
    font-weight: 800;
    color: white;
    font-family: 'Inter', sans-serif;
    line-height: 1;
    margin-bottom: 8px;
  }

  .stat-unit {
    font-size: 1.1rem;
    font-weight: 600;
    color: rgba(255,255,255,0.85);
    margin-left: 2px;
  }

  .stat-label {
    font-size: 0.85rem;
    color: rgba(255,255,255,0.8);
    font-weight: 600;
  }

  /* ========== ASP GRID ========== */
  .section-sub {
    text-align: center;
    color: var(--text-muted);
    font-size: 0.95rem;
    margin-bottom: 40px;
  }

  .asp-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
    max-width: 960px;
    margin: 0 auto;
  }

  @media (min-width: 640px) {
    .asp-grid { grid-template-columns: 1fr 1fr; }
  }

  @media (min-width: 900px) {
    .asp-grid { grid-template-columns: 1fr 1fr 1fr; }
  }

  /* ========== SLACK CTA ========== */
  .slack-cta {
    background: linear-gradient(135deg, #1A1A2E 0%, #2C3E50 100%);
    padding: 80px 0;
  }

  .slack-cta__inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
    align-items: center;
  }

  @media (min-width: 768px) {
    .slack-cta__inner { grid-template-columns: 1fr 1fr; }
  }

  .slack-cta__image img {
    width: 100%;
    max-width: 380px;
    display: block;
    margin: 0 auto;
    filter: drop-shadow(0 8px 32px rgba(0,0,0,0.3));
  }

  .slack-cta__title {
    font-size: 1.8rem;
    color: white;
    margin-bottom: 24px;
    line-height: 1.4;
  }

  .slack-cta__list {
    list-style: none;
    margin-bottom: 32px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .slack-cta__list li {
    color: rgba(255,255,255,0.85);
    font-size: 0.95rem;
    font-weight: 500;
  }

  .slack-cta__list strong { color: #FFB347; }

  .btn--white {
    background: white;
    color: var(--primary);
    font-weight: 800;
    border-radius: var(--radius-full);
    padding: 16px 40px;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    transition: all 0.2s ease;
    box-shadow: 0 4px 16px rgba(255,255,255,0.2);
  }

  .btn--white:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(255,255,255,0.3);
  }

  /* ========== MISC ========== */
  .delay-1 { transition-delay: 0.1s; }
  .mt-4 { margin-top: 1rem; }
</style>

<script>
  // Body loaded
  document.body.classList.add('loaded');

  // Scroll Observer
  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) entry.target.classList.add('visible');
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('.scroll-fade-up').forEach(el => observer.observe(el));
</script>
