---
import { footerLinks, siteConfig } from '../config/site';
---

<footer class="footer">
  <div class="container">
    <div class="footer__grid">
      <div class="footer__brand">
        <a href="/" class="footer__logo">
          <span class="footer__logo-icon">📚</span>
          <span>PT国試コミュニティ</span>
        </a>
        <p class="footer__description">
          理学療法士を目指す学生のための学習コミュニティ。
          国試対策から実習・就職まで一貫してサポートします。
        </p>
        <p class="footer__contact">
          お問い合わせ: <a href={`mailto:${siteConfig.CONTACT_EMAIL}`}>{siteConfig.CONTACT_EMAIL}</a>
        </p>
      </div>
      
      <div class="footer__links">
        <h4 class="footer__heading">サービス</h4>
        <ul class="footer__list">
          {footerLinks.main.map((link) => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>
      
      <div class="footer__links">
        <h4 class="footer__heading">法的事項</h4>
        <ul class="footer__list">
          {footerLinks.legal.map((link) => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>
    </div>
    
    <div class="footer__disclaimer">
      <p>
        ※当サイトは合格を保証するものではありません。学習成果は個人の努力と状況により異なります。
      </p>
    </div>
    
    <div class="footer__bottom">
      <p class="footer__copyright">
        &copy; {new Date().getFullYear()} PT国試コミュニティ. All rights reserved.
      </p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-text);
    color: white;
    padding: var(--space-16) 0 var(--space-8);
  }
  
  .footer__grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: var(--space-12);
    margin-bottom: var(--space-12);
  }
  
  .footer__logo {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-weight: 700;
    font-size: var(--text-xl);
    color: white;
    text-decoration: none;
    margin-bottom: var(--space-4);
  }
  
  .footer__logo:hover {
    color: var(--color-primary-light);
  }
  
  .footer__logo-icon {
    font-size: var(--text-2xl);
  }
  
  .footer__description {
    color: var(--color-text-muted);
    font-size: var(--text-sm);
    line-height: 1.8;
    margin-bottom: var(--space-4);
  }
  
  .footer__contact {
    color: var(--color-text-muted);
    font-size: var(--text-sm);
  }
  
  .footer__contact a {
    color: var(--color-primary-light);
  }
  
  .footer__heading {
    color: white;
    font-size: var(--text-base);
    font-weight: 600;
    margin-bottom: var(--space-4);
  }
  
  .footer__list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .footer__list li {
    margin-bottom: var(--space-2);
  }
  
  .footer__list a {
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: var(--text-sm);
    transition: color var(--transition-fast);
  }
  
  .footer__list a:hover {
    color: white;
  }
  
  .footer__disclaimer {
    padding: var(--space-6);
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-md);
    margin-bottom: var(--space-8);
  }
  
  .footer__disclaimer p {
    color: var(--color-text-muted);
    font-size: var(--text-sm);
    text-align: center;
    margin: 0;
  }
  
  .footer__bottom {
    padding-top: var(--space-8);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    text-align: center;
  }
  
  .footer__copyright {
    color: var(--color-text-muted);
    font-size: var(--text-sm);
    margin: 0;
  }
  
  @media (max-width: 768px) {
    .footer__grid {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }
  }
</style>
